{
  "name": "max-pixels",
  "version": "0.1.0",
  "description": "Web-based multiplayer space exploration and trading game",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "http-server . -p 3000 -o -c-1",
    "server": "node server/gameServer.js",
    "dev:multiplayer": "npm run server & npm run dev",
    "build": "node scripts/build.js",
    "lint": "eslint src/**/*.js",
    "lint:fix": "eslint src/**/*.js --fix",
    "test": "echo '🚀 Opening Max-Pixels Test Runner...' && http-server . -p 3001 -o test/test-runner.html -c-1",
    "test:headless": "node test/headless-test-runner.js",
    "test:ci": "npm run lint && npm run test:headless",
    "clean": "rm -rf dist/",
    "serve:prod": "npm run build && http-server dist -p 8080 -o"
  },
  "keywords": [
    "game",
    "space",
    "trading",
    "multiplayer",
    "svg",
    "web"
  ],
  "author": "Max-Pixels Dev Team",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "eslint": "^9.34.0",
    "http-server": "^14.1.1",
    "terser": "^5.43.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
